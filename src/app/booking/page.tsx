import { BookingScreen } from '@/screens';

export const metadata = {
  title: 'Rendez-vous - NOWIS',
  description: 'Réserve ta consultation gratuite pour ton projet.',
};

export default function BookingPage() {
  return <BookingScreen />;
}
